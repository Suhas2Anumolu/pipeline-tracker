# Postgres connection string, e.g. from Supabase, Railway, Neon, or local Docker
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pipeline_tracker?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-openssl-rand-base64-32"

# Optional OAuth providers (add if you wire them into src/lib/auth.ts)
GITHUB_ID=""
GITHUB_SECRET=""
